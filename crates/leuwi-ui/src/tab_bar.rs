// Tab bar widget — Chrome-style tabs that serve as the window header.
// No separate titlebar — the tab bar IS the header.
//
// Phase 1: Using basic Makepad buttons (see app.rs)
// Phase 2: Custom tab bar widget with drag-to-reorder, close button on hover,
//          window drag on empty area, rounded tab shapes.
