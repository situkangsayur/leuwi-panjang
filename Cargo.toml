[package]
name = "leuwi-panjang"
version = "0.1.0"
edition = "2021"

[dependencies]
makepad-widgets = { git = "https://github.com/makepad/makepad", branch = "main" }
portable-pty = "0.8"

[profile.release]
opt-level = 3
lto = true
strip = true
